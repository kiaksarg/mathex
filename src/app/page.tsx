import { Center } from '@chakra-ui/react'

import MathEx from '../components/MathEx' // Adjust the import path if necessary

export default function Home() {
  return (
    <main>
      <Center p={4}>
        <MathEx />
      </Center>
    </main>
  )
}
